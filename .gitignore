# IDE
.idea/
*.iml
*.iws
*.ipr

# Node
node_modules/
package-lock.json

# OS
.DS_Store
Thumbs.db

# Maven
target/

# Local secrets (real credentials) — never commit
application-local.yml

# PPT generation scripts
generate-pptx.js
*.pptx

# Playwright debug artifacts
.playwright-mcp/

# Deploy: SQL dump 与上传用 jar 不入库（真实凭据文件 application-local.yml 已被全局规则排除）
deploy/sql/*.gz
deploy/*/app.jar
